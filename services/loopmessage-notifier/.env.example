# LoopMessage organization API key.
# Dashboard -> select org -> API -> Settings (enable the API feature first).
LOOPMESSAGE_API_KEY=""

# Sender name id to send from. Omit for "auto".
# Fetch the list with: GET /health/upstream?token=<INTRO_WEBHOOK_TOKEN>
LOOPMESSAGE_SENDER_ID=""

# Shared secret the Masterinbox app must present as ?token=... — this
# endpoint sits on a public Railway domain, so without it anyone who guesses
# the URL can spend the LoopMessage balance.
INTRO_WEBHOOK_TOKEN=""

# Optional. Force a delivery channel: imessage | sms | rcs | whatsapp.
# Leave unset so LoopMessage picks iMessage and falls back on its own.
LOOPMESSAGE_CHANNEL=""

# Optional. Country code assumed for bare local numbers in
# client_team_members.phone. The production rows are US.
DEFAULT_COUNTRY_CODE="1"

# Optional. Set to 1 to plan and log without actually sending.
DRY_RUN=""

# Optional. Override the LoopMessage API base (tests/staging).
LOOPMESSAGE_BASE_URL=""
